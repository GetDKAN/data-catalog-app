import React from 'react';
import { DataTable } from '@civicactions/data-catalog-components';

const ResourcePreview = ({dist}) => {
  const { data } = dist;
  return(
    <div>
      {/* <DataTable

      /> */}
    </div>
  )
}

export default ResourcePreview;
